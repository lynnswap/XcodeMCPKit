name: CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - README.md
      - LICENSE
  pull_request:
    paths-ignore:
      - README.md
      - LICENSE
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package-build:
    name: Package Build (macOS)
    runs-on: macos-26
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          fetch-depth: 0

      - name: Select latest Xcode
        id: toolchain
        run: |
          set -euo pipefail
          xcode_path="$(
            ruby -e 'candidates = Dir["/Applications/Xcode*.app"].filter_map { |path| version = File.basename(path)[/\AXcode[_-](\d+(?:\.\d+)*)\.app\z/, 1]; version ? [version.split(".").map(&:to_i), path] : nil }; abort "No versioned Xcode apps found" if candidates.empty?; puts candidates.max_by(&:first).last'
          )"
          sudo xcode-select -s "${xcode_path}/Contents/Developer"
          xcodebuild -version
          swift --version
          cache_id="$(xcodebuild -version | tr '\n ' '--' | tr -cd '[:alnum:]._-')"
          echo "cache-id=${cache_id}" >> "$GITHUB_OUTPUT"

      - name: Restore SwiftPM build cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .build
          key: spm-build-${{ runner.os }}-${{ runner.arch }}-${{ steps.toolchain.outputs.cache-id }}-${{ hashFiles('Package.swift', 'Package.resolved') }}-${{ github.sha }}
          restore-keys: |
            spm-build-${{ runner.os }}-${{ runner.arch }}-${{ steps.toolchain.outputs.cache-id }}-${{ hashFiles('Package.swift', 'Package.resolved') }}-

      - name: Restore source modification times
        run: ./scripts/restore-source-mtimes.sh

      - name: Build package tests
        run: swift build --build-tests -Xswiftc -strict-concurrency=minimal

  package-tests:
    name: Package Tests (macOS, ${{ matrix.shard }})
    needs: package-build
    runs-on: macos-26
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        shard:
          - RuntimeCoordinatorProcessRoutingTests
          - RuntimeCoordinatorInitializationTests
          - RuntimeCoordinatorRecoveryTests
          - RuntimeCoordinatorCatalogTests
          - RuntimeCoordinatorWindowCatalogTests
          - RuntimeCoordinatorWindowRoutingTests
          - RuntimeCoordinatorSchedulingTests
          - remaining
    steps:
      - name: Checkout
        uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1

      - name: Select latest Xcode
        id: toolchain
        run: |
          set -euo pipefail
          xcode_path="$(
            ruby -e 'candidates = Dir["/Applications/Xcode*.app"].filter_map { |path| version = File.basename(path)[/\AXcode[_-](\d+(?:\.\d+)*)\.app\z/, 1]; version ? [version.split(".").map(&:to_i), path] : nil }; abort "No versioned Xcode apps found" if candidates.empty?; puts candidates.max_by(&:first).last'
          )"
          sudo xcode-select -s "${xcode_path}/Contents/Developer"
          xcodebuild -version
          swift --version
          cache_id="$(xcodebuild -version | tr '\n ' '--' | tr -cd '[:alnum:]._-')"
          echo "cache-id=${cache_id}" >> "$GITHUB_OUTPUT"

      - name: Restore built package tests
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .build
          key: spm-build-${{ runner.os }}-${{ runner.arch }}-${{ steps.toolchain.outputs.cache-id }}-${{ hashFiles('Package.swift', 'Package.resolved') }}-${{ github.sha }}
          fail-on-cache-miss: true

      - name: Run package tests
        run: |
          case "${{ matrix.shard }}" in
            remaining)
              ./scripts/ci-test-watchdog.sh swift test \
                --skip-build \
                --no-parallel \
                --filter 'ControlPlaneAuthorityTests|JSONRPCResponseRouterTests|ManagedUpstreamSlotTests|ProxyUpstreamRequestRuntimeTests|ToolCatalogStartupLogFormatterTests|UpstreamReadinessTests' \
                -Xswiftc -strict-concurrency=minimal
              ./scripts/ci-test-watchdog.sh swift test \
                --skip-build \
                --no-parallel \
                --skip ProxyRuntimeCoordinatorTests \
                -Xswiftc -strict-concurrency=minimal
              exit 0
              ;;
            *)
              filter='${{ matrix.shard }}'
              ;;
          esac
          ./scripts/ci-test-watchdog.sh swift test \
            --skip-build \
            --no-parallel \
            --filter "${filter}" \
            -Xswiftc -strict-concurrency=minimal

      - name: Run process tests
        if: matrix.shard == 'remaining'
        run: |
          XCODE_MCP_RUN_PROCESS_TESTS=1 ./scripts/ci-test-watchdog.sh swift test \
            --skip-build \
            --no-parallel \
            --filter XcodeMCPProcessRuntimeTests \
            -Xswiftc -strict-concurrency=minimal
          XCODE_MCP_RUN_PROCESS_TESTS=1 ./scripts/ci-test-watchdog.sh swift test \
            --skip-build \
            --no-parallel \
            --filter ProxyStdioAdapterTests \
            -Xswiftc -strict-concurrency=minimal
