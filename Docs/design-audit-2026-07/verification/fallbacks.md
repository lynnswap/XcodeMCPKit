## [WEAKENED] inferredUnambiguousOwnerProcessID: when window-owner lookup fails, ownership is inferred from (a) single usable route or (b) tool-name-to-catalog intersection (RuntimeCoordinator+XcodeProcessRouting.swift:652-684) — ownership truth silently shifts from window index to catalog content (second source of truth / fail-fast violation).

EVIDENCE: Mechanics confirmed: single-usable-route inference at Sources/XcodeMCPProxyKit/Internal/Session/Runtime/RuntimeCoordinator+XcodeProcessRouting.swift:660-666, catalog-intersection inference at :668-683. Interpretation refuted on two points. (1) Trigger is mischaracterized: the guard at :656-657 requires ALL unresolved owner-bound requests to have NO owner hint (hasNoOwnerHint, :686-699). Hint-less requests are .unresolved by construction (cachedOwnerResolution returns .unresolved at :1358-1359 when neither workspacePath nor tabIdentifier is present) — the window index has nothing to look up, so no 'window-index truth' exists to be overridden. A request WITH a hint that fails lookup never reaches inference; it goes to live refresh (:532-535) and then hard reject 'unable to resolve Xcode window owner' (:560-568). (2) It is intended, test-pinned normal-path behavior: Tests/ProxyRuntimeCoordinatorTests/RuntimeCoordinatorTests.swift:9585 ownerBoundToolWithoutOwnerHintRoutesWhenSingleProcess and :9626 ownerBoundToolWithoutOwnerHintRoutesWhenSingleCatalogCandidate pin exactly branches (a) and (b); :11217 ownerBoundToolRejectsWhenOwnerCannotBeResolvedAfterRefresh pins the reject path. Classification: (i) documented-by-test normal-path disambiguation for hint-less calls (not in prose docs — docs/maintainer-architecture.md does not describe owner routing). Residual (iii)-flavored scenario that keeps this from full refutation: the intersection uses processToolSurfaceStore.processIDsHavingTool (:668-671), so while a second Xcode's catalog has not loaded yet (routes can be usable before their catalog exists — Tests :10032 ownerHintRoutesBeforeProcessToolCatalogIsAvailable), a genuinely ambiguous hint-less call can appear falsely unambiguous (usableCandidates.count == 1 at :680) and route to the only cataloged Xcode instead of rejecting. Since the client supplied no hint, no client-specified target is violated; the alternative is reject.

## [WEAKENED] Catalog load failure returns the previous surface as success (RuntimeCoordinator+ControlPlane.swift:183-216), including a 25ms polling loop waiting for another load; and a failed task is re-marked successful if a surface re-appeared (:284-304).

EVIDENCE: Three parts, each weaker than claimed. (1) The generic-catch 'return previous surface as success' branch (Sources/XcodeMCPProxyKit/Internal/Session/Runtime/RuntimeCoordinator+ControlPlane.swift:183-194) is unreachable dead code: currentSurface is an immutable let captured at :128; if it had a non-nil sourceUpstream the function already returned at :146-158 (partial-surface return with cacheableAsCanonical:false) before entering the do-block at :160; and a non-nil surface always has non-nil sourceUpstream (ProcessToolSurfaceStore.swift:438-452 — sourceUpstream is nil only when catalogs are empty, which yields a nil surface). So the guard at :184-186 always rethrows. (2) The 25ms polling loop (:197-216) runs only in the CancellationError catch (:168-182) when Task.isCancelled == false, i.e., the in-flight RPC was cancelled externally by a superseding activation attempt (RPCHandle.cancel via ProcessRouteReadinessStore.swift:215, :285, :424; rethrow chain at RuntimeCoordinator+ControlPlane.swift:882-883, :260-261). It returns a surface only if it exactly covers exposedProcessIDs (:205 surface.processIDs == exposedProcessIDs) — i.e., it waits for a concurrent winner's fresh, complete surface (coalescing) — and rethrows CancellationError at deadline (:175, :212). It never returns a stale partial 'previous' surface. (3) The :284-304 failure-to-success conversion is reachable only when the failed process has a catalog in the store at failure time (:284-286). Batch entry excludes already-cataloged processes (:141-142 uncachedRoutes; :390-396 missingRoutes filter), so any such catalog was recorded DURING the load window by a concurrent loader under the same broker generation and a current route lease (record guards at :533-540 processToolsCatalogLoadLeaseIsCurrent, :519-521 activeRoute.id == routeID, :639-653 generation-fenced mutation) — fresh data, not resurrection of stale state; the lease check at :276-283 gates the path. The keep-surviving-catalogs-on-route-failure semantics are test-pinned: Tests/ProxyRuntimeCoordinatorTests/RuntimeCoordinatorTests.swift:5662 sessionManagerToolsListKeepsCachedProcessCatalogWhenFreshRouteFails (asserts the served result keeps the cached process catalog and canonical is NOT cached, :5725-5726), :7241 sessionManagerSkipsFailureCleanupWhenRouteLeaseIsStale, :7307 sessionManagerStaleMissingRouteFailureDoesNotClearExistingProcessSurface. Classification: (i) documented-by-test partial-surface semantics for :146-158 and :284-304; (ii) dead code for the :183-194 success branch. No completable trace in which a load failure silently serves data that a fresh load would not also serve.

## [WEAKENED] handleUpstreamExit primary-index fallback chain ends in slot 0 fabrication: globalInit.primaryInitUpstreamIndex ?? initializeSourceUpstream() ?? 0 (RuntimeCoordinator+UpstreamRouting.swift:326-328).

EVIDENCE: Code confirmed at Sources/XcodeMCPProxyKit/Internal/Session/Runtime/RuntimeCoordinator+UpstreamRouting.swift:326-328. Interpretation weakened by reachability + convention + consequence analysis. Reachability: the '?? 0' fires only when there is no in-flight primary init (primaryInitUpstreamIndex = state.primaryInitializePhase.upstreamIndex, InitializeManager.swift:516-519) AND canonicalBrokerState.initializeSourceUpstream() == nil. initializeSourceUpstream is set and cleared strictly together with initializeResult (CanonicalBrokerState.swift:100-108 set together; :126-132 clearInitialize and :147-156 reset clear together; no independent clear), so initializeSourceUpstream == nil implies hadGlobalInit == false (hadGlobalInit = brokerState.initializeResult() != nil, InitializeManager.swift:516) — i.e., only the cold/uninitialized state. Consequence: primaryUpstreamIndex only selects a restart strategy (:329-350); in the cold state the only effect is that upstream 0's exit triggers startEagerInitializePrimary (:330-331 via !globalInit.hadGlobalInit), which re-derives the actual candidate via primaryInitializeUpstreamIndex() (RuntimeCoordinator+Initialization.swift:6-15, 109-127) — no request is ever routed to a fabricated slot 0. Convention: index 0 IS the designated default primary when process routing is disabled (Initialization.swift:110-112 'return excludedUpstreamIndices.contains(0) ? nil : 0'), and the identical chain appears at Initialization.swift:129-134 (currentPrimaryInitializeUpstreamIndex) and UpstreamRouting.swift:1102-1104 — a repo-wide default, though uncommented. Classification: closest to (i) convention default in single-upstream mode; in multi-upstream cold state it is an undocumented heuristic, but no masking trace completes: the in-flight-primary exit case is handled separately (:267-306), and the worst outcome (a non-zero upstream exiting cold gets no warm restart from this branch, :335) loses no cached init state. The triplicated chain is a structural smell (no single owner of 'current primary'), not a fabrication defect.

## [REFUTED] ProcessToolSurfaceStore legacy record() fabricates ProcessRouteID(instanceGeneration: 0) (ProcessToolSurfaceStore.swift:87-95); check whether any PRODUCTION call site still uses this legacy path.

EVIDENCE: Code exists as described: Sources/XcodeMCPProxyKit/Internal/Session/Runtime/ProcessToolSurfaceStore.swift:81-95, fabricated routeID at :88 (ProcessRouteID(processID:, instanceGeneration: 0)). But the 'production fabrication path' interpretation is refuted: rg over Sources/ finds ZERO production callers of processToolSurfaceStore.record( — the only production recording path is recordCatalog with the real activeRoute.id at Sources/XcodeMCPProxyKit/Internal/Session/Runtime/RuntimeCoordinator+ControlPlane.swift:642-649, guarded by activeRoute.id == routeID (:519-521). All callers of legacy record() are tests: Tests/ProxyRuntimeCoordinatorTests/RuntimeCoordinatorTests.swift:5525, :5596, :7575 and Tests/XcodeMCPProxyInternalTestSupport/RuntimeCoordinatorTestSupport.swift:274 (seedProcessToolCatalogs). Production route IDs are always minted with a real generation by ProcessRouteStore (ProcessRouteStore.swift:127-135 init, :220-229 reconcile), so a generation-0 ID never becomes a live route ID; the sibling convenience init XcodeProcessRoute(target:upstreamIndices:) with instanceGeneration 0 (XcodeProcessRoute.swift:28-35, used at RuntimeCoordinator+XcodeProcessReconciliation.swift:221, :241) is likewise re-keyed by the store before exposure. Classification: (ii) production-dead — a test-only seeding helper on a production type. It cannot mask production failures; residual note is hygiene only (a test-support API living in the production type bypasses the routeID/generation guards that production recording enforces).

## [REFUTED] WindowOwnerIndex.proxyTabIdentifier synthesizes a hash for identities never registered (WindowOwnerIndex.swift:121-136) — fabrication / second source of truth.

EVIDENCE: Code confirmed: Sources/XcodeMCPProxyKit/Internal/Session/Runtime/WindowOwnerIndex.swift:121-136 falls back to Identity(...).proxyTabIdentifier when no registered identity matches. The second-source-of-truth interpretation is refuted: proxyTabIdentifier is a pure deterministic function — SHA256 over 'pid\0rawTabIdentifier\0workspacePath' with a fixed prefix (WindowOwnerIndex.swift:22-34, computed in Identity.init at :11-20). Both branches of :126-135 therefore produce byte-identical strings for the same inputs; no divergent second value can exist (the lookup arm is in fact redundant). The sole production caller is rewriteXcodeListWindowsResultForClients (RuntimeCoordinator+XcodeProcessRouting.swift:906-916), and both of its call sites register the exact same window entries immediately before rewriting: RuntimeCoordinator.swift:1444-1449 (recordXcodeWindowOwners then rewrite) and RuntimeCoordinator+XcodeProcessRouting.swift:145-149 (recordXcodeWindowOwners(fromOrderedRouteResults:) then rewrite), with record and rewrite parsing identical entries via Self.windowEntries. The fallback can only fire in a narrow race where a concurrent owner removal (e.g., removeXcodeWindowOwners, :345-356) lands between record and rewrite — and even then it emits the same identifier registration would have produced. Failure is not masked downstream: resolving an unregistered proxy identifier fails loudly with 'stale or unknown XcodeMCPKit tabIdentifier' (:1288-1291, :1406-1408), which is the correct outcome for an owner that just disappeared. Classification: (i)/(ii) — a deterministic pure-function computation, not an inference; the redundant lookup-then-compute shape is a simplification opportunity, not a fail-fast violation.

