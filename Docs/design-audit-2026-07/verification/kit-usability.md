## [CONFIRMED] XcodeMCPError conforms to neither LocalizedError nor CustomStringConvertible anywhere in the module

EVIDENCE: Sources/XcodeMCPKit/XcodeMCPError.swift:9 declares `public enum XcodeMCPError: Error, Equatable, Sendable` only. Module-wide grep for `extension XcodeMCPError` across all of Sources/ and Tests/ returns zero hits; grep for LocalizedError/CustomStringConvertible in Sources/XcodeMCPKit hits nothing (all CustomStringConvertible conformances are in XcodeMCPProxyKit/ProxyBuildInfoTool/verifier, other modules). Corroborating: the repo's own consumers work around it — Sources/XcodeMCPProxyToolVerifier/main.swift:1268-1279 falls back to String(describing:) because LocalizedError check fails for XcodeMCPError, and Tests/XcodeMCPKitTests/XcodeMCPTests.swift:1202 defines its own LocalizedError wrapper.

## [CONFIRMED] A stale/missing proxy discovery file surfaces as XcodeMCPError.invalidRequest, a misleading category for an environment/setup problem

EVIDENCE: Sources/XcodeMCPKit/Internal/ClientRuntime/StreamableHTTPXcodeMCPTransport.swift:64-68 throws MCPBridgeRuntimeError.invalidRequest("Streamable HTTP discovery file is missing, stale, or invalid: ..."); Sources/XcodeMCPKit/XcodeMCP.swift:404-405 maps .invalidRequest verbatim to XcodeMCPError.invalidRequest. XcodeMCPError.swift:13-16 documents invalidRequest as "The client rejected a request before sending it... describes the invalid input", while XcodeMCPError.swift:32-35 documents transportUnavailable as "The underlying bridge process or transport was unavailable" — the natural category for this failure. No code path remaps discovery failures to transportUnavailable.

TRACE: 1) Caller uses XcodeMCPConfiguration(transport: .streamableHTTPProxyDiscovery()) with the proxy not running (XcodeMCP.swift:95-99). 2) XcodeMCP.init (XcodeMCP.swift:196) reaches case .streamableHTTPDiscoveryFile at XcodeMCP.swift:222-227 and calls StreamableHTTPXcodeMCPTransport.start(discoveryFile:). 3) discoveryResolver.endpoint(from:) returns nil because the file is missing or record.pid is dead (StreamableHTTPXcodeMCPTransport.swift:29-37). 4) start throws MCPBridgeRuntimeError.invalidRequest (StreamableHTTPXcodeMCPTransport.swift:64-67). 5) catch at XcodeMCP.swift:230-232 calls publicError, which maps it at XcodeMCP.swift:404-405 to XcodeMCPError.invalidRequest — caller sees an 'invalid input' error for a proxy-not-running environment condition.

## [CONFIRMED] MCPJSONValue has no subscript accessors and no unlabeled conversion inits for String/Int variables; intValue and integerValue are exact duplicates both returning Int64

EVIDENCE: Grep for `subscript` across all of Sources/ returns zero hits. All MCPJSONValue extensions live in Sources/XcodeMCPKit/MCPJSONValue.swift (grep confirmed: lines 82, 129-168, 171, 226, 278 only). Public inits are init(from:):41, init(jsonObject:):91 (labeled, throwing), init(encoding:):113 (labeled, throwing), plus literal-protocol inits (129-168) which only apply to literals; the sole unlabeled init `init(_ value: JSONValue)` at :227 is package-scoped and takes the internal JSONValue type. integerValue (MCPJSONValue.swift:197-200) and intValue (:203-205, forwards to integerValue) both return Int64? with the identical doc comment "Returns the integer when this value is an integer JSON number." — no Int-typed variant exists.

## [CONFIRMED] Every MCPContent case requires a raw: payload so constructing text content means writing the text twice (README does exactly that), and MCPToolResult has no aggregate text accessor

EVIDENCE: Sources/XcodeMCPKit/MCPDomainTypes.swift:68 (.text(String, raw:)), :73 (.image(data:mimeType:raw:)), :78 (.resource(uri:text:mimeType:raw:)), :81 (.raw). No convenience factory exists (grep for `static func text`/extensions of MCPContent finds only the package init(json:) at :260). The raw duplication is load-bearing, not optional: MCPContent.encode(to:) encodes rawValue (:90-92) and MCPToolResult synthesizes its wire object from content.map(\.rawValue) (:153, :361), and XcodeMCPTestRuntime returns result.raw to the client (XcodeMCPTestRuntime.swift:309-312), so a mismatched raw corrupts the round-trip. README example at Sources/XcodeMCPKit/README.md:170-173 writes "Result text" twice. MCPToolResult public members are exactly content/structuredContent/isError/raw (MCPDomainTypes.swift:113-130); grep for allText/joinedText/textContent/var text across XcodeMCPKit and XcodeMCPKitTesting returns zero hits.

## [CONFIRMED] listTools() reads only result["tools"] and ignores nextCursor pagination

EVIDENCE: Sources/XcodeMCPKit/XcodeMCP.swift:266-272: single request("tools/list") with no params, reads result.objectValue?["tools"]?.arrayValue, returns. Grep for `nextCursor` across all Sources/ returns zero hits (the only `cursor` matches are CLI argument parsing in XcodeMCPProxyKit). InitializedMCPClientSession has no pagination logic either. Mitigating context only: the public request("tools/list", params:) escape hatch (XcodeMCP.swift:337) lets a caller paginate manually.

TRACE: 1) A spec-compliant MCP server paginates tools/list and returns {tools: [first page], nextCursor: "abc"}. 2) listTools() at XcodeMCP.swift:267 sends tools/list with no cursor param. 3) Line 268 extracts only ["tools"]; nextCursor is dropped without inspection. 4) Line 271 returns the first page as the complete catalog — tools beyond page one are silently invisible, and a `tools.contains(where:)` gate like the README quickstart (README.md:43) wrongly reports a tool as unavailable.

## [CONFIRMED] No public connection-state observability on XcodeMCP (no isClosed, no state stream, no disconnect callback); transport close reasons surface only as later request errors

EVIDENCE: Complete public surface of XcodeMCP (XcodeMCP.swift:183-368, verified by grepping every `public` declaration in the module): init, listTools, callTool, request, notify, close — no state property, stream, or callback. XcodeMCPTransportEvent and XcodeMCPTransport are package-scoped (Internal/ClientRuntime/XcodeMCPTransport.swift:3-13), and grep confirms no public declaration exists anywhere under Sources/XcodeMCPKit/Internal. Close reasons ("protocol violation: ...", "process exited with status ...") are yielded at XcodeMCPTransport.swift:55,59,64.

TRACE: 1) mcpbridge dies; UpstreamProcessXcodeMCPTransport yields .closed("process exited with status N") (XcodeMCPTransport.swift:58-61). 2) InitializedMCPClientSession.handle sets isClosed=true and fails only in-flight requests with transportUnavailable(reason) (InitializedMCPClientSession.swift:296-303). 3) An idle client learns nothing — no API exposes the transition. 4) The next call hits ensureOpen() (InitializedMCPClientSession.swift:286-290) and throws MCPBridgeRuntimeError.closed, surfaced as bare XcodeMCPError.closed (XcodeMCP.swift:402-403) — the exit-status reason string is lost entirely for requests issued after the close.

## [CONFIRMED] XcodeMCPTestRuntime.makeClient(configuration:) silently ignores configuration.transport; no recordedToolCalls() accessor exists despite the public ToolCall type

EVIDENCE: Sources/XcodeMCPKitTesting/XcodeMCPTestRuntime.swift:158-164: makeClient builds XcodeMCPTestTransport and passes it to the package init XcodeMCP(configuration:transport:) (XcodeMCP.swift:235-252), which reads only clientName/clientVersion/capabilities/requestTimeout — configuration.transport is never inspected, and neither the code nor the doc comment (:153-157) warns about it. Grep for `recordedToolCalls` across the entire repo returns zero hits; the runtime's only accessors are recordedMessages() (:212) and recordedCloseCount() (:217). ToolCall (public, :45-70) is consumed by the ToolHandler typealias (:119), so it is not purposeless, and tests can manually decode tools/call params from recordedMessages() — mitigations, but no decoded recorded-calls accessor exists.

TRACE: 1) Test writes `runtime.makeClient(configuration: XcodeMCPConfiguration(transport: .streamableHTTP(endpoint: someURL)))` expecting the HTTP path to be exercised. 2) makeClient (XcodeMCPTestRuntime.swift:161-163) unconditionally substitutes XcodeMCPTestTransport. 3) XcodeMCP(configuration:transport:) (XcodeMCP.swift:235) uses the substituted transport; configuration.transport.storage is never read. 4) The test passes against the in-memory transport with no error or warning that its transport selection was discarded.

