## [CONFIRMED] CanonicalBrokerState.generation serves three roles at once (initialize-cache validity, tools-catalog validity, window-owner-change signal): clearInitialize and clearToolsCatalog bump the same counter (CanonicalBrokerState.swift:126-145).

EVIDENCE: Single counter: Sources/XcodeMCPProxyKit/Internal/Session/ControlPlane/CanonicalBrokerState.swift:61 (one `generation` field, doc comment at 57-60: 'Bumped on every clear/reset'); clearInitialize bumps it at :130, clearToolsCatalog at :142, reset at :154. Role 1 (tools-catalog validity): syncCanonicalToolsCatalog(onlyIfGeneration:) CanonicalBrokerState.swift:117-119; catalog loads snapshot it as startGeneration (ControlPlaneCoordinator.swift:302) and check it at completion (:560); surface mutations gate on it (RuntimeCoordinator.swift:998-1003); per-process catalog load leases gate on it (RuntimeCoordinator+ControlPlane.swift:708-722) and scheduled retries (:425, :450). Role 2 (initialize-cache invalidation feeding the same counter): clearInitialize is invoked via invalidateControlPlaneGeneration(clearInitialize:true) (RuntimeCoordinator.swift:976-989) from e.g. RuntimeCoordinator.swift:851-854, RuntimeCoordinator+Initialization.swift:802-805, RuntimeCoordinator+UpstreamRouting.swift:1084-1088, RuntimeCoordinator+XcodeProcessReconciliation.swift:273-277 — each bump cancels/invalidates catalog and window loads even when clearToolsCatalog is false. Role 3 (window-owner-change signal): all four owner-index mutation sites signal exclusively through this counter via invalidateControlPlane(clearToolsCatalog:true) (RuntimeCoordinator+XcodeProcessRouting.swift:349-355, 366-372, 821-827, 849-855). Window loads also key validity off the same counter (ControlPlaneCoordinator.swift:342, :594), so consumers cannot distinguish which of the three events bumped it. Nuance (does not weaken the conflation): the generation is a bump-source for initialize invalidation but is never read to validate the initialize cache itself — syncCanonicalInitialize has no generation guard (CanonicalBrokerState.swift:100-108) and InitializeManager checks initializeResult() presence directly (InitializeManager.swift:218, 226, 294); i.e., a clearInitialize bump is observable only by catalog/window-load consumers.

## [CONFIRMED] Window-owner index changes call invalidateControlPlane(clearToolsCatalog: true), which bumps the generation, which causes ControlPlaneCoordinator to fail in-flight catalog-load waiters with CancellationError; net effect: a window update aborts unrelated in-flight tools/list catalog loads.

EVIDENCE: Call sites re-verified at HEAD: RuntimeCoordinator+XcodeProcessRouting.swift:349-355 (removeXcodeWindowOwners(forProcessID:)), 366-372 (clearXcodeWindowOwners), 821-827 (recordXcodeWindowOwners(fromOrderedRouteResults:)), 849-855 (recordXcodeWindowOwners(from:upstreamIndex:...)). Refutation (a) fails: the guard is only `changed` (index content differs, WindowOwnerIndex.swift:55-76), and the canonical catalog is a union of per-process upstream tools/list results with zero dependency on windowOwnerIndex (ProcessToolSurfaceStore.swift:421-429; owner counts are debug-only, ProcessToolSurfaceStore.swift:324-343; exposure comes from processRouteExposure which reads only route/health state, RuntimeCoordinator+XcodeProcessRouting.swift:253-255, 395-408) — so a tab/window topology change invalidates a catalog it cannot affect. Refutation (b) fails: no layer retries — sharedToolsList (RuntimeCoordinator.swift:1379-1417) propagates the error; LocalMCPResponder.handleLocalResult (LocalMCPResponder.swift:181-214) and the batch path (ClientMCPRequestExecutor+LocalHandling.swift:381-409) map it to a client-visible JSON-RPC error; ErrorMapper has no CancellationError case so it falls to default (-32000, "upstream timeout") (RuntimeCoordinator+ControlPlane.swift:33-52); the loader-internal CancellationError recovery (RuntimeCoordinator+ControlPlane.swift:168-182) is defeated because cancelToolsCatalogLoad resumes waiters with CancellationError directly and cancels the loader task (Utilities.swift:51-61). Refutation (c) fails: startGeneration is brokerState.generation() (ControlPlaneCoordinator.swift:302), the same counter clearToolsCatalog bumps (CanonicalBrokerState.swift:142). Caveat: if the load completes and its completion reads the generation before the bump, waiters get the result — the abort is not guaranteed for loads finishing within the race window, but any load still in flight when the bump lands (control-plane RPCs to Xcode upstreams) is aborted via either path.

TRACE: 1) MCP client sends tools/list -> LocalMCPResponder.handle (LocalMCPResponder.swift:125-151) -> sharedToolsList (RuntimeCoordinator.swift:1379) -> controlPlaneCoordinator.toolsCatalog (RuntimeCoordinator.swift:1398-1402). 2) Cache miss (brokerState.toolsCatalogRaw()==nil, ControlPlaneCoordinator.swift:126) -> ensureToolsCatalogForegroundLoad -> startToolsCatalogLoad records startGeneration=G (ControlPlaneCoordinator.swift:302) and issues upstream tools/list RPCs (RuntimeCoordinator+ControlPlane.swift:72-98); the client's waiter is registered (ControlPlaneCoordinator.swift:140-149, 409-438). 3) Concurrently the user opens/closes an Xcode tab or window; a subsequent XcodeListWindows tool call records owners (RuntimeCoordinator.swift:1444 -> RuntimeCoordinator+XcodeProcessRouting.swift:830-848): index differs -> changed=true -> invalidateControlPlane(reason:"xcode_window_owners_updated", clearToolsCatalog:true) (:849-855). 4) invalidateControlPlane (RuntimeCoordinator.swift:1656-1677) -> invalidateControlPlaneGeneration (:976-989) -> canonicalBrokerState.clearToolsCatalog() bumps generation G->G+1 (CanonicalBrokerState.swift:142) and schedules coordinator.cancelLoadsStartedBeforeGeneration(G+1) (RuntimeCoordinator.swift:1671-1676). 5) On the actor, cancelLoadsStartedBeforeGeneration(G+1) (ControlPlaneCoordinator.swift:278-283 -> Utilities.swift:15-49): toolsCatalogLoad.startGeneration=G < G+1 -> load removed and cancelToolsCatalogLoad resumes every waiter (including the client's) with CancellationError (Utilities.swift:51-61). [Race alternative: if the load finishes first and observes G+1, completeToolsCatalogLoad sees completedUnderCurrentGeneration=false (ControlPlaneCoordinator.swift:560) and resumes waiters with CancellationError (:579-580); the loaded catalog is also discarded by onlyIfGeneration (CanonicalBrokerState.swift:117-119).] 6) CancellationError propagates through toolsCatalog -> awaitControlPlaneOperation -> sharedToolsList throws -> LocalMCPResponder catch (LocalMCPResponder.swift:195-206) -> ErrorMapper default branch (RuntimeCoordinator+ControlPlane.swift:51) -> the connected MCP client receives a JSON-RPC error response code -32000 message "upstream timeout" for its tools/list request, even though all upstreams were healthy and the catalog content never changed.

