## [CONFIRMED] start()/startAndWriteDiscovery() are synchronous and internally block on NIO futures via .wait(), with no async start variant in the public surface; the README quickstart usage would be called from async context, blocking a cooperative thread.

EVIDENCE: XcodeMCPProxyServer+Startup.swift:14 (`public func start() throws -> Endpoint`); XcodeMCPProxyServer.swift:517 (`public func startAndWriteDiscovery() throws -> Endpoint`); bindChannels XcodeMCPProxyServer.swift:611-637 with `.wait()` at :613, :619, :626, :634, called directly from startListening at XcodeMCPProxyServer+Startup.swift:45 on the caller's thread (no dedicated-thread dispatch). Public start surface exhaustively: only these two sync funcs — the sole `public func start() async` is XcodeMCPProxyStdioAdapter.swift:370, a different type (stdio adapter, not the server); `public static func run(...) async` (XcodeMCPProxyRunners.swift:20-32) internally calls the sync start. README Sources/XcodeMCPProxyKit/README.md:34-56 shows `try server.startAndWriteDiscovery()` in the same snippet as `try await server.wait()` and `try await server.shutdown()`, i.e. an async context. Stronger: the library's own launcher does exactly this — XcodeMCPProxyServer+Launcher.swift:94-116, `startServer` is async and calls `try server.startAndWriteDiscovery()` at :115.

TRACE: 1) Embedder runs README quickstart inside async main (README.md:46-55, `try await` in same scope). 2) `startAndWriteDiscovery()` (XcodeMCPProxyServer.swift:517) executes on a cooperative-pool thread. 3) It calls `startListening()` (XcodeMCPProxyServer+Startup.swift:20), which calls `bindChannels(using:)` at :45. 4) `bindChannels` calls `bootstrap.bind(host:port:).wait()` (XcodeMCPProxyServer.swift:613 or :619/:626/:634), which blocks the calling cooperative thread until the bind completes on the NIO event-loop thread created in init (:500). Same trace applies to the shipped CLI facade via XcodeMCPProxyServer+Launcher.swift:115 (async `startServer`). No deadlock (ELG is a separate thread), but a cooperative executor thread is blocked for the bind duration; if the event loop stalls, the cooperative thread is held indefinitely.

## [CONFIRMED] MultiThreadedEventLoopGroup is created in init and only released by shutdown(); a constructed-but-never-started server leaks the NIO thread (no deinit).

EVIDENCE: XcodeMCPProxyServer.swift:497-506, group created at :500 (`MultiThreadedEventLoopGroup(numberOfThreads: 1)`) in the designated init reached by the public convenience init (:489-495). Sole release path: shutdown() (:546-569) -> shutdownEventLoopGroup() (:590-600) -> `group.shutdownGracefully` (:592). `rg "deinit|syncShutdownGracefully|shutdownGracefully"` over Sources/XcodeMCPProxyKit shows no deinit in XcodeMCPProxyServer.swift and no other group-shutdown site. Mitigating context (does not refute): shutdown() is callable on a never-started instance — beginShutdown (:760) tolerates empty channels and nil sessionManager — so a careful embedder can avoid the leak, but nothing enforces it.

TRACE: 1) Embedder constructs `XcodeMCPProxyServer(configuration:)` (XcodeMCPProxyServer.swift:489) — e.g. start later aborts, or config validation fails before start. 2) init at :500 spawns a MultiThreadedEventLoopGroup with 1 live NIO thread. 3) The instance is dropped without calling shutdown(). 4) There is no deinit; `group.shutdownGracefully` (:592) is only reachable from shutdown() (:559). 5) The NIO thread is never terminated — one leaked thread per constructed-and-dropped server instance.

## [CONFIRMED] Production type RuntimeCoordinator embeds RuntimeCoordinatorTestHooks (~10 closures) including processToolCatalogSurfaceUpdatePassedInitialGenerationCheck, wired into production code paths, with nothing stripping them in release builds.

EVIDENCE: Struct at Internal/Session/Runtime/RuntimeCoordinator.swift:38-91; exactly 10 optional closure properties at :39-56, incl. processToolCatalogSurfaceUpdatePassedInitialGenerationCheck at :46 (a hook that fires after an internal generation guard, i.e. exposes guard sequencing). Stored as production property :436, default all-nil init param :560, assigned :614. Invoked in production paths: RuntimeCoordinator.swift:775, :960, :1014, and bridged into UpstreamSlotScheduler's own testHooks at :662-666; RuntimeCoordinator+Initialization.swift:550, 593, 692, 767, 779; +Health.swift:150, 172; +XcodeProcessRouteActivation.swift:72, 100, 138, 182, 219, 233, 283, 301, 377; +ControlPlane.swift:241, 273. Release stripping: `rg "#if DEBUG"` over Sources/XcodeMCPProxyKit returns zero matches — hooks compile into release unconditionally. Production composition root XcodeMCPProxyServer.swift:455-463 omits testHooks (all nil at runtime), so hooks are inert-but-present in production; non-nil hooks are supplied only by tests. Same pattern repeats in sibling types (UpstreamSlotSchedulerTestHooks, DocumentationProviderManagerTestHooks, RefreshCodeIssuesCoordinator TestHooks), confirming it is a package-wide idiom, not an isolated case.

CAUSALITY BOUNDARY: This confirms release inclusion and test coupling to internal sequencing. It does not prove that the hooks caused the 5/20 flake-fix PRs; that relationship is only a plausible correlation. Any replacement should be justified by owner-boundary contract coverage and measured determinism, not by claiming a proven causal history.

TRACE: Structural claim; behavioral trace not applicable. Runtime effect in release is nil-checked no-op calls (e.g. RuntimeCoordinator.swift:960 `testHooks.processToolCatalogSurfaceUpdatePassedInitialGenerationCheck?()` executes on every process tool-catalog surface update).

## [WEAKENED] ControlPlane.DebugMirror duplicates control-plane state as a mirror that can drift from primary state.

EVIDENCE: What it stores: ControlPlane.DebugSnapshot (Internal/Session/ControlPlane/ControlPlaneSupport.swift:51-60 — phase, canonicalInitialize/ToolsSourceUpstream, canonicalReady, upstreamHandshakeStates, waiterCounts, inFlightControlPlaneRequests, lastIncompatibility); DebugMirror class :64-152 holds `snapshot: DebugSnapshot?` plus predicate-waiters with continuations. Writer: only ControlPlaneCoordinator.syncDebug() (+LoadPromotion.swift:161-174, overwrite at :173), called at ~14 sites inside ControlPlaneCoordinator plus two explicit resyncs from RuntimeCoordinator (+ControlPlane.swift:788, 793). Readers: production — exactly one, RuntimeCoordinator.debugSnapshot() (+UpstreamRouting.swift:543) feeding GET /debug/upstreams via HTTPControlService.swift:63-77; tests — waitForSnapshot as a synchronization primitive (~25 sites in Tests/ProxyRuntimeCoordinatorTests/RuntimeCoordinatorTests.swift, plus Tests/ProxyHTTPGatewayTests/HTTPConcurrencyTests.swift:481). Drift: staleness is possible — canonicalBrokerState mutates outside ControlPlaneCoordinator (RuntimeCoordinator.swift:931, 982-993, 1024, 1030; +Initialization.swift:334; +ControlPlane.swift:830, 1424) and the mirror refreshes only when syncDebug is manually invoked (the RuntimeCoordinator-side resyncs at +ControlPlane.swift:788/793 prove the freshness invariant is maintained by convention). WEAKENED because: each syncDebug rebuilds the snapshot wholesale from primary state (brokerState.snapshot(), +LoadPromotion.swift:162-172), so it is a stale read-model, not a divergent second source of truth; and no production behavior reads it — routing reads canonicalBrokerState directly (+UpstreamRouting.swift:257, 327). Residual risk is confined to /debug/upstreams output accuracy and test synchronization (a stale mirror can hang/flake waitForSnapshot-based tests). Structure/naming smell, not a production correctness risk.

## [CONFIRMED] No embedder observability: upstream health/debug snapshot reachable only via HTTP endpoints; logger not injectable; discovery-file write failure swallowed into a warning.

EVIDENCE: (a) HTTP-only: HTTPRoute.swift:12-19 — GET /health (:14-15), GET /debug/upstreams (:16-17); the only production consumer of RuntimeCoordinator.debugSnapshot is HTTPControlService.swift:63-77; the coordinator handle `sessionManager` is internal (XcodeMCPProxyServer.swift:479) and the enumerated public surface of XcodeMCPProxyServer (init :489, start +Startup.swift:14, startAndWriteDiscovery :517, wait :537, shutdown :546, Endpoint :343, LifecycleError :382, PortInUseError, launch statics, run) exposes no health/debug accessor — an embedder must make a loopback HTTP request to its own in-process server. (b) Logger: XcodeMCPProxyServer.swift:475 `let logger: Logger = ProxyLogging.make("server")` — hardcoded, no init parameter; XcodeMCPProxyServerConfiguration (XcodeMCPProxyServer.swift:12-234) has no logging field; the bootstrap helper is `package`-scoped (XcodeMCPProxyLogging.swift:3), unreachable to embedders. Nuance that softens severity but does not refute: ProxyLogging.make is just Logger(label:) (Internal/Session/Support/ProxyLogging.swift:32-34) and the embed path never self-bootstraps (doc comment XcodeMCPProxyRunners.swift:9-11), so a host can still control backend/level process-wide via swift-log's global LoggingSystem.bootstrap — but not per-instance, and cannot set label/metadata. (c) Discovery: XcodeMCPProxyServer.swift:647-667 — write failure caught and demoted to logger.warning (:658-666); additionally makeRecord returning nil is a fully silent early return (:648-655); startAndWriteDiscovery (:517-530) returns Endpoint with no discovery-status signal and no error, so the documented adapter-discovery contract (README.md:58-60) can silently not hold.

TRACE: For (c): 1) Embedder calls startAndWriteDiscovery() with discovery.fileURL on an unwritable path (XcodeMCPProxyServer.swift:517). 2) writeDiscovery (:521 -> :647) hits the catch at :658 and logs a warning; if the swift-log backend was never bootstrapped by the host, the default handler still writes to stderr but nothing programmatic surfaces. 3) The method returns a valid Endpoint (:529). 4) Local stdio adapters that resolve the endpoint via the discovery file (README.md:58-60) fail to find the server; the embedder observed a successful start and has no API to detect the discrepancy.
