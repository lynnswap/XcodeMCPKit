## [CONFIRMED] The narrow launch-plan families are 72 of 191 public keyword lines (~38%). Thin executable entrypoints call only run() facades, while the bundled production path consumes plans through package-internal launchers. The documented external custom-launcher story remains incomplete because Launcher is package, ExistingServerController internal, isAddressAlreadyInUse package, and bootstrapLogging package.

EVIDENCE: The module has 191 keyword-marked public declaration lines (215 matches of '^\s*public' minus 24 false positives inside a string literal at Sources/XcodeMCPProxyKit/Internal/Session/DocumentationProvider.swift:1540-1591). Launch families contain 72 lines on the same scale (XcodeMCPProxyServer+Launch.swift = 29 incl. XcodeMCPProxyProductMetadata; +PortDiagnostics.swift = 6; adapter launch decls in XcodeMCPProxyStdioAdapter.swift = 23; installer launch decls in XcodeMCPProxyInstaller.swift = 14). Implicit enum cases are excluded from both numerator and denominator. Executable entrypoints call only run(): Sources/XcodeMCPProxyServer/XcodeMCPProxyServer.swift:10, Sources/XcodeMCPProxyCLI/XcodeMCPProxyCLI.swift:8, Sources/XcodeMCPProxyInstall/main.swift:4. Production then reaches resolveLaunchPlan through XcodeMCPProxyServer+Launcher.swift:60, XcodeMCPProxyStdioAdapter.swift:738, and XcodeMCPProxyInstaller.swift:449. Access levels re-verified at HEAD: 'package struct Launcher' Sources/XcodeMCPProxyKit/XcodeMCPProxyServer+Launcher.swift:7; internal 'struct ExistingServerController' +ExistingServerController.swift:9; 'package static func isAddressAlreadyInUse' +PortDiagnostics.swift:53; 'package static func bootstrapLogging' +Launch.swift:267. README sells the story at Sources/XcodeMCPProxyKit/README.md:24-27 ('custom launcher needs the same parsing, dry-run, force-restart, and version behavior as the bundled executables'). Refutations failed: (a) start()/startAndWriteDiscovery() throw untyped NIO bind errors (+Startup.swift:13 doc, :45-48; LifecycleError.failedToBind at :51-54 only fires when zero channels bound); repo-wide search for public terminate/detect/restart API matches only the LaunchOptions.forceRestart data fields (+Launch.swift:57,60). (b) No public logging bootstrap (XcodeMCPProxyLogging.swift:3 is package); generic swift-log bootstrap is possible via the Logging module per the run() doc (XcodeMCPProxyRunners.swift:9-11), but env-based level parity (Internal/Session/Support/ProxyLogging.swift:21) is not public — minor, does not restore force-restart/port-in-use capability. (d) Tests/PublicProductContractTests/PublicProductContractTests.swift is compile-only: it constructs PortInUseError with hardcoded pids [123] (lines 994-998) and never implements restart or detection; the actual force-restart/port-in-use behavior tests use @testable import and the package Launcher (Tests/ProxyCLITests/ServerRunnerTests.swift:3, 182-215, 351-370).

TRACE: External package imports XcodeMCPProxyKit (public product, Package.swift:26-29) and follows README.md:24-27+126-142 to build a custom launcher -> resolveLaunchPlan returns plan.options.forceRestart == true for --force-restart (+Launch.swift:57, 250) -> to honor it the launcher must terminate the existing server; the only implementations are the package Launcher dependencies (.live wiring +Launcher.swift:30-44) -> internal ExistingServerController (+ExistingServerController.swift:9) -> internal ExistingProxyServerProcessController (Sources/XcodeMCPProxyKit/Internal/Process/ExistingProxyServerProcessController.swift); none visible outside the package, so the flag is read-only data -> launcher then calls public startAndWriteDiscovery(); on EADDRINUSE bindChannels rethrows the raw NIO error (+Startup.swift:45-48, XcodeMCPProxyServer.swift:611-637) -> classifying it requires package isAddressAlreadyInUse (+PortDiagnostics.swift:53) and populating PortInUseError.processIdentifiers requires package detectExistingServerProcessIDs (+Launcher.swift:12,40-42); externally PortInUseError (+PortDiagnostics.swift:6) is only a message formatter -> external launcher cannot reproduce the bundled executables' documented force-restart or port-in-use diagnostics.

## [CONFIRMED] All three LaunchPlans model action payload as optionals (configuration?/endpoint?), forcing three synthetic can't-happen guards in the repo's own launchers and a force-unwrap in the README example.

EVIDENCE: Optionals: server LaunchPlan.configuration? at Sources/XcodeMCPProxyKit/XcodeMCPProxyServer+Launch.swift:74; adapter LaunchPlan.configuration?/endpoint? at Sources/XcodeMCPProxyKit/XcodeMCPProxyStdioAdapter.swift:223,226; installer LaunchPlan.configuration? at Sources/XcodeMCPProxyKit/XcodeMCPProxyInstaller.swift:58. Guards: +Launcher.swift:98-103 ('server launch plan is missing configuration'); XcodeMCPProxyStdioAdapter.swift:777-780 ('adapter launch plan is missing endpoint') — note the claim cited the file as 'StdioAdapter.swift'; the guard is in XcodeMCPProxyStdioAdapter.swift (Stdio/StdioAdapter.swift is a different internal file), line numbers match; XcodeMCPProxyInstaller.swift:462-466 ('installer launch plan is missing configuration'). README force-unwrap: Sources/XcodeMCPProxyKit/README.md:139 'XcodeMCPProxyServer(configuration: plan.configuration!)'. Can't-happen verified: resolveLaunchPlan returns nil payload only for .showHelp/.showVersion and always populates it for .start/.dryRun/.install (server +Launch.swift:205-224 vs 245-261; adapter XcodeMCPProxyStdioAdapter.swift:498-517 vs 554-571 where endpoint is resolved or the function throws; installer XcodeMCPProxyInstaller.swift:202-219 vs 221-227 where parseLaunchConfiguration returns non-optional). Refutation angle (c) fails to overturn: the per-action invariant is documented only in doc comments (+Launch.swift:72-73, XcodeMCPProxyStdioAdapter.swift:221-225, XcodeMCPProxyInstaller.swift:56-57), not enforced by the type (LaunchAction carries no payload), so the guards/force-unwrap remain required at every consumer.

TRACE: Launcher.run (+Launcher.swift:60-63) obtains plan exclusively from resolveLaunchPlan -> for plan.action == .start, resolveLaunchPlan constructed the plan at +Launch.swift:254-261 with configuration built at :245 (non-nil by construction) -> startServer must still unwrap at +Launcher.swift:98-103 and throws a synthetic LaunchResolutionError that no argv/environment input can trigger; identical dead branches at XcodeMCPProxyStdioAdapter.swift:777-780 and XcodeMCPProxyInstaller.swift:462-466; external README consumers hit the same type hole and are shown a force-unwrap (README.md:139).

## [WEAKENED] XcodeMCPProxyInstaller has 40 public declaration lines; its only in-repo production caller outside the module is the 12-line install main, while a module-README embedder story exists and external usage is unknown.

EVIDENCE: Exactly 40 lines match '^\s*public' in Sources/XcodeMCPProxyKit/XcodeMCPProxyInstaller.swift. Sources/XcodeMCPProxyInstall/main.swift is exactly 12 lines and calls only XcodeMCPProxyInstaller.run (main.swift:4-9); repo-wide search finds no other in-repo production caller outside the module. However, an embedding example is documented in the module README (Sources/XcodeMCPProxyKit/README.md:202-226), and the compile-only contract fixture touches installer surface (Tests/PublicProductContractTests/PublicProductContractTests.swift:878,951-956,1011-1018,1039-1047). The repo cannot prove or disprove external consumers, so “no embedder story” and “sole production consumer” were too strong.
